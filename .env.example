# Web RAG System 环境变量配置模板
# 复制此文件为 .env 并填入真实的配置值

# =============================================================================
# Google API 配置 (必需)
# =============================================================================
# Google Gemini API 密钥
# 获取地址: https://aistudio.google.com/
# 格式: AIza...
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# 应用配置 (可选)
# =============================================================================
# 应用运行端口 (默认: 7860)
PORT=7860

# 应用运行主机 (默认: 127.0.0.1，Spaces 环境会自动设置为 0.0.0.0)
HOST=127.0.0.1

# =============================================================================
# 文档处理配置 (可选)
# =============================================================================
# 文档分块大小 (默认: 1000)
CHUNK_SIZE=1000

# 文档分块重叠 (默认: 200)
CHUNK_OVERLAP=200

# 最大文档数量 (默认: 无限制)
MAX_DOCUMENTS=100

# =============================================================================
# 模型配置 (可选)
# =============================================================================
# 优先使用的 Gemini 模型 (默认: 自动选择)
PREFERRED_MODEL=gemini-2.5-flash-preview-05-20

# 嵌入模型 (默认: models/embedding-001)
EMBEDDING_MODEL=models/embedding-001

# 生成温度 (默认: 0.1)
TEMPERATURE=0.1

# 最大输出令牌数 (默认: 2048)
MAX_OUTPUT_TOKENS=2048

# =============================================================================
# 向量数据库配置 (可选)
# =============================================================================
# ChromaDB 持久化目录 (默认: ./chroma_db)
CHROMA_PERSIST_DIRECTORY=./chroma_db

# 检索相似文档数量 (默认: 4)
RETRIEVAL_K=4

# =============================================================================
# 调试和日志配置 (可选)
# =============================================================================
# 启用调试模式 (默认: False，本地开发可设为 True)
DEBUG=False

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# Hugging Face Spaces 环境变量 (自动设置，无需手动配置)
# =============================================================================
# SPACE_ID - HF Spaces 自动设置的空间 ID
# SPACE_AUTHOR_NAME - HF Spaces 自动设置的作者名
# SPACE_REPO_NAME - HF Spaces 自动设置的仓库名

# =============================================================================
# 安全提示
# =============================================================================
# ⚠️  重要提醒:
# 1. 请勿将包含真实 API 密钥的 .env 文件提交到版本控制系统
# 2. 在 Hugging Face Spaces 中，请使用 Secrets 功能设置 GOOGLE_API_KEY
# 3. 确保 .env 文件已添加到 .gitignore 中
# 4. 定期轮换 API 密钥以确保安全性